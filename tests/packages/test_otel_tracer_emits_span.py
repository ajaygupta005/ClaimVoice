"""Component 09 - shared-observability tracer emits a span."""
import pytest


def test_traced_function_emits_span():
    """Stub: decorate a function with @traced, capture exported spans, assert one was emitted."""
    pytest.skip("implement after shared-observability is built")
