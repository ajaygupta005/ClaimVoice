"""Component 09 - @observe_anthropic decorator records a Langfuse generation."""
import pytest


def test_observe_anthropic_records_generation():
    """Stub: mock anthropic.Anthropic().messages.create; assert Langfuse client called with model + tokens + latency."""
    pytest.skip("implement after shared-observability is built")
