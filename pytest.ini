[pytest]
testpaths = tests services/telephony/tests eval/tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    integration: integration tests (may need running services)
    e2e: end-to-end tests (slow, may hit external APIs)
